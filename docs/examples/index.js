import { Actribute } from "../../dist/esm/actribute";
const act = new Actribute();

function tabd(tabPanels, tabButtons) {

}


act.register('tabs', (element) => {
    // Use the < and > keys to move to next and pravious tags respectively.
});










act.process();
